Improvement backlog — from review of the rvscN GCC backend, tests, and main.typ
(2026-07-11). Each task is a self-contained file in improvements/ with the
instructions and context needed to complete it independently.

Suggested order (correctness first, then docs, then polish):

 1. [done] pr38051 -Os ra corruption
    Fixed via NORA_REGS class + "yr" constraint on scratches of the three
    _sc1 shift patterns (lshrsi3_sc1, ashrsi3_sc1, ashlsi3_sc1_var).
    pr38051.c passes at -O0/-O1/-O2/-O3/-Os.

 2. [done] improvements/02-behav-all-opt-levels.md
    tests/sc1/behav.py already loops OPT_LEVELS = [-O0,-O1,-O2,-O3,-Os] (see
    commit 5e0ec12); confirmed with `just behav` — 30/30 pass across all
    five levels for both branch.c and slt.c.

 3. improvements/03-differential-behav-layer.md
    Restore reference-compiler differential comparison (self-validating tests
    can't catch symmetric miscompiles; Method chapter still promises it).
    STILL OPEN: behav.py only checks `spike` exit code == 0 for the sc1
    binary; there is no reference-compiler comparison yet.

 4. [done] improvements/04-xor-derivation-rewrite.md
    main.typ already rewrites the derivation to (a|b)-(a&b) (see commit
    8c7b7fa): §sc1-xor proof, cost table, §sc1-example worked example, the
    define_expand listing, and both ISA test-table cells all updated; new
    corner case #4 "Register Aliasing in Dataflow-Disconnected Synthesis
    Sequences" added at §sc1-cc-aliasing (main.typ:1440). *xorhi3_noxor's
    a+b-2(a&b) form intentionally left unmerged, footnoted as tracked under
    item 6. Follow-up cleanup: the rvsc0 behavioral test-table cell
    (main.typ:1586) and stale "De Morgan" comments in four test sources
    (tests/sc0/tests/isa/logic.c, tests/sc0/tests/behav/{logic,not}.c,
    tests/sc1/tests/behav/logic.c) still described the old synthesis;
    reworded to describe the cross-check formulas as independent identities
    rather than implying the compiler still synthesizes XOR via De Morgan.

 5. [done] improvements/05-delete-dead-branch-templates.md
    Removed unreachable raw-asm SLT/SLTU branch templates (undeclared t2-t4
    clobbers) from *branch<mode>'s GE/LT/GEU/LTU arms; replaced with
    gcc_unreachable() (the pattern condition already guarantees TARGET_SLT
    whenever those arms run). No length attr arms referenced TARGET_SLT, so
    none needed removal. sc1: 95/95 ISA tests, 30/30 behav tests (all opt
    levels) pass. Torture suite: 7749/8420 pass, 670 skipped, 1 pre-existing
    unrelated failure (ashrdi-1.c -O2, DImode/long long shift — out of scope
    per "No 64-bit Integers" limitation, triggers the documented branch-
    relaxation caveat; unaffected by this change since it only touches
    already-dead code paths).

 6. [done] improvements/06-factor-synthesis-helpers.md
    Deduplicated slt/sltu synthesis into riscv_emit_slt_synth (cbranch +
    cstore) and the no-LUI constant-construction idiom into
    riscv_emit_const_no_lui (9 call sites: zero_extendhi expand + split,
    extend<SHORT><SUPERQI>, movhi, ashrsi3_sc1 — one more site than the
    original doc listed). Unified *xorhi3_noxor onto the (a|b)-(a&b)
    identity used for SImode XOR. Verified as a pure refactor: sc0/sc1
    generated assembly byte-identical before/after across the full ISA
    test suites; sc1 95/95 ISA + 30/30 behav, sc0 60/60 ISA + 40/40 behav
    all pass; sc2 rebuilds clean (no test suite yet). Torture suite:
    7734/8420 pass, 685 skipped, 1 pre-existing unrelated failure
    (ashrdi-1.c -O2, same DImode-shift gap noted in item 5, confirmed via
    git-stash A/B test to predate this refactor). Committed as
    ada605483e4.

 7. improvements/07-rtx-costs-mem-slt.md
    Extend riscv_rtx_costs beyond shifts: byte/half mem ops (~75-100),
    slt/sltu (~65), xor (3). Should reduce -O2/-O3 torture timeouts.

 8. improvements/08-document-mlui-flag.md
    -mlui exists in riscv.opt + riscv.cc hooks but is missing from every
    flag table (main.typ @tbl-flags, CLAUDE.md).

 9. improvements/09-torture-reporting.md
    Exact (not "~") torture numbers via --csv; separate KNOWN_SLOW from
    compile skips; XFAIL pr38051 -Os; report torture_isa.py in Results.

10. improvements/10-harness-self-test.md
    Negative-control canary: reference-compiler output must FAIL the
    allowlist, empty mnemonic extraction is an error (no vacuous passes).

11. improvements/11-size-perf-data-scripts.md
    Script the Program Size / Performance measurements (objdump counts,
    spike --log-commits) and fill the TODO tables in ch-results.

12. improvements/12-claude-md-refresh.md
    CLAUDE.md drift: test layout, pk-based behav flow, opt levels, torture
    runners, XOR identity, mlui, rvsc0.h, post-reload shift patterns.
