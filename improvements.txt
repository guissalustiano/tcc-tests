Improvement backlog — from review of main.typ against commits 369680f..4f423ef
(2026-07-27). Fresh start: the previous backlog is dropped in full. Each task is
a self-contained file in improvements/ with the context needed to complete it
independently.

All seven are documentation defects: main.typ drifted behind the backend across
five commits. Nothing here is a code bug. Item 05 is the only one needing a
measurement run; the rest are edits to main.typ.

Suggested order (contradictions first, then arithmetic, then polish):

 1. [done] improvements/01-purge-const-shift-loop-prose.md
    Replaced the §Future Work paragraph that proposed the constant-shift unroll
    already shipped in c708790 (and quoted a $2b$ target the implementation beat
    at $b$) with the genuinely open item: constant-count SRL/SRA are unrolled
    but still linear in word width (159/195 worst case), a bitmask-and-recombine
    formulation could improve on that, and the variable-count forms still pay 6
    instructions per iteration to re-materialize the back-edge. Split main.typ:
    1466 from two categories into three, inserting constant-count shifts
    (straight-line, $b$ / 159 / 195) between constant-length and variable-length.
    Rewrote the §Limitations claim at 1578 that synthesis "expands a shift into a
    counted loop": sequential-ness holds in both cases, but the loop exists only
    for variable counts, and the constant-count limitation is linearity, not
    looping. Qualified 1550 and 1568 as variable-count where they attribute cost
    to back-edge re-materialization and to the 187-instruction depth. Counts
    re-measured against the installed toolchain rather than taken from the task
    file: shr1=159, sra1=195, shift3=4 (also shr3=151, sra3=185, shr31=39,
    sra31=45), matching @tab-synthesis-cost. §6 and the appendices untouched;
    typst compile clean.

 2. [done] improvements/02-fix-torture-skip-breakdown.md
    Replaced the seven-category run-on sentence at main.typ:1442 with
    @tbl-torture-skips, which partitions the 54 KNOWN_UNSUPPORTED sources
    instead of overlapping them: printf 40, expensive 5, __int128 3, libm 2,
    sys/mman.h 2, _Decimal 1, x87 asm 1. The old text attributed all 54 to the
    printf family (actually 40), then listed 12 more as if they were additional
    (reading as 66 against a table saying 54), and omitted the 2 libm sources
    added by 4f423ef entirely. Counts derived mechanically from the
    KNOWN_UNSUPPORTED comment blocks in tests/sc1/torture_behav.py rather than
    from the old prose. libm now shares the "build configuration gap" sentence
    with printf, since both fail as undefined references at link. The static-
    sweep paragraph is untouched and stays consistent: its 35 front-end
    rejections are __int128 (3x5) + sys/mman.h (2x5) + x87 (1x5) + _Decimal
    (1x5), and printf/libm correctly do not appear there because they compile
    fine and only fail at link. The -O0 trio sentence was kept verbatim, so
    item 07 part 4 (naming the three sources and showing the mechanism rather
    than asserting it) is still open. typst compile clean.

 3. [done] improvements/03-remove-false-differential-claims.md
    Took the "stop claiming it" resolution: no reference RV32I build exists, so
    the differential wording was replaced by the mechanism actually implemented
    — self-validating programs that check each result against a value derived
    independently in the same program (a hand-computed constant or an algebraic
    identity over other operations), reporting the first failure via the exit
    code (rvsc1, under pk) or the `tohost` token (rvsc0, bare-metal HTIF).
    The task file named three passages; there were six. Beyond §7 Discussion
    (1567), §8 Contributions (1581) and §8 Results Summary (1585, "differential"
    dropped), the claim also appeared in *both* abstracts — the Portuguese
    resumo at 188 ("execução diferencial ... compilador de referência RV32I")
    and the English one at 217 — and in §4 Validation at 432, which stated the
    two-compiler comparison as the method rather than as a design option, so the
    task's Verify step required aligning it. Contributions also lost "the
    computed results are indistinguishable from those of a full-ISA compiler"
    in favor of "match the values the C semantics require", per the task's note
    that the original is defensible only because the expected values are hand-
    computed. §4's self-validating description at 1291 was already correct and
    is untouched; the correctness claim itself is not weakened, only its
    provenance corrected. grep for differential/diferencial/reference RV32I/
    compilador de referência over main.typ now returns nothing; typst compile
    clean.

 4. [done] improvements/04-reframe-slt-synth-ice-paragraph.md
    Verified the premise in the gcc fork before editing, since the whole task
    turns on it: *branch<mode>_slt_synth is this work's own pattern (first
    appears with the sc1 slt synthesis, ada605483e4), and the fix commit
    16de133341e0 says the operands "had a predicate ... but no constraint string
    at all -- unlike the sibling *branch<mode> pattern, which has always used
    r/rJ". The sibling is upstream's; the defective one is ours. So "pre-
    existing, unrelated" was wrong on both counts, and main.typ:1468 now says
    the ICEs were "both in a pattern this work itself introduced", modeled on
    the upstream sibling, closing with the point the paragraph was underselling:
    the defect lay in this work's own synthesis machinery, and surfaced only
    once the harness stopped folding compile failures into one skip count --
    the validation methodology exposing a defect in the code it was built to
    check. Technical narrative (LRA / no constraint to act on / post-reload
    split, plus the pr10352-1.c and arith-rand.c witnesses) kept verbatim.
    Did not merge with the following paragraph as the task floated; instead
    de-duplicated across the two, since that paragraph's three-shortcomings
    list already carries the mechanism. Its opening lost the clause "which at
    the time folded every compile failure into an undifferentiated skip count",
    which attributed both defects to skip-folding, and now names the two escape
    routes separately. Same split applied at the §Test Strategies end
    (main.typ:1298): skip-folding hid the missing-constraint defect, while the
    register-clobber defect surfaced as execution timeouts. The one remaining
    "unrelated" in §7 (main.typ:1466) is the technical sense -- an unrelated
    live value IRA misallocated -- not a provenance claim, so it stays.
    typst compile clean.

 5. improvements/05-remeasure-embench-after-unroll.md
    @tbl-embench-size and @tbl-embench-perf predate c708790 and the t1
    register-clobber fix. The x4.94 and x13.7 headline figures are quoted in six
    places. Needs a re-measure, a recorded compiler revision, and possibly a new
    causal explanation if the overhead drops.

 6. improvements/06-cost-table-and-sh-mask-corrections.md
    §6: the sh mask is described as `lui 0x10; addi -1` but the cost table lists
    sh for rvsc0, which has no lui; @tab-synthesis-cost has no row for the
    synthesized jump though §Contributions lists JMP; "eighteen distinct
    operations" no longer matches the 20-row table; the table now mixes static
    and dynamic counts in one column.

 7. improvements/07-results-chapter-small-corrections.md
    Copy-level: `sub x0, rs` missing its destination (twice), the same
    illustration using rvsc3 in one place and rvsc2 in another, appendix listing
    lengths off by one (181/216, not 182/217), and an unverified claim about the
    three -O0-only skips.
